# Private key of the account that deploys the Covenant stack. Never commit the real .env file.
DEPLOYER_PRIVATE_KEY=0xyourprivatekeyhere

# Arc testnet — chain id 5042002, RPC hardcoded in foundry.toml (https://rpc.testnet.arc.io).
# Gas is paid in USDC (18 decimals on Arc, despite the symbol). Fund via https://faucet.circle.com
